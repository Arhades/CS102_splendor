import java.util.*;

public class GemCost {
    private Map<GemColour, int> cost;

    public int getCost(GemColour colour) {
        return cost.getOrDefault(colour, 0);
    }

    public int totalCost() {
        // Looi Le pls help
    }

    public boolean isCoveredBy(/* code */) {
        // Looi Le pls help
    }
}